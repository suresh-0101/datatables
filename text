function initOrderTable(jsonColumns) {
  $('#orderTable').DataTable({
    destroy: true,
    scrollX: true,
    searching: false,
    paging: true,
    data: [],
    columns: buildColumnsFromJson(jsonColumns)
  });
}
